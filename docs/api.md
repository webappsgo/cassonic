# API Reference

cassonic exposes a REST API and GraphQL API.

## REST API

Interactive API documentation is available at `/swagger/` when the server is running.

## GraphQL

Interactive GraphQL explorer is available at `/graphql/` when the server is running.

## Authentication

API endpoints require authentication. See [Admin Panel](admin.md) for token management.
