# Integrations

## Tor Hidden Service

cassonic automatically enables a Tor hidden service when the `tor` binary is found on the system or installed in the container. The `.onion` address is displayed in the startup banner.

Configuration is managed entirely by the cassonic binary — no manual `torrc` editing required.

## External Services

No external services are required or enabled by default.
